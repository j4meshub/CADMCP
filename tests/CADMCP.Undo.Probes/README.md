# 禁用：撤销隔离诊断程序

## 后续验证（2026-09-03，用户重启后）

旧 Run 入口仍禁用。新增 RunCurrent 使用用户明确指定的当前测试文档和token，不新建/切换文档，一次运行一个场景。必须先通过 safety_mismatch；启动要等外层MCP命令完全退出且CAD空闲。所有原生命令回调内部捕获异常，主线程/文档/空间校验失败即停止。每阶段独立写入 artifacts/UNDO_PROBE_*.json；结果未知不得重试。

已验证错误身份安全退出，以及当前宿主下的原生U事件等待。这个测试器不是通用插件API，也不随Bundle部署。用户本次明确指定的DWG是专用测试图；不把本次授权推广至其他文档。具体对照和候选结果见 artifacts/UNDO_ROOT_CAUSE_AND_CANDIDATE_2026-09-03.md。下面保留首次失败与修正前的要求作为历史记录。

此项目不属于插件产物，也不随 Bundle 发布。2026-09-03 首次启动导致 CAD 致命错误，`Run` 入口现已被 IsQuarantined 阻断；禁止删除阻断后直接重跑。

转储中的异常为 `System.Exception: Independent test document changed; stopped`，栈经过本诊断 DLL 与 AutoCAD `<Module>.myCallback(Void*)`。活动文档保护异常从命令回调逃逸，外部 await 的 try/catch 未能安全处理，这是诊断程序自身缺陷，不是原连续撤销问题的根因证据。

已局部修正：Command 的原生托管回调内部捕获异常，返回后由托管流程报错；Add 新图后等待 Idle 再校验活动身份。但入口继续禁用，尚未真实验证这些改动，也没有隔离测试通过结果。

重新设计前须满足：

- 独立空白测试进程，不同时打开业务图。
- 每个 AutoCAD 回调内部兜底，含事件/清理/异步入口；避免任何异常越过宿主边界。
- 用主线程调度、文档激活完成信号、quiescent 状态与身份共同确认执行条件，不凭 Add 返回就立即写入。
- 阶段结果在执行前后持久记录，超时/异常停止，不自动重试未知操作。
- 先验证“故意身份不匹配能安全停止”这一负例，再恢复任何写入/UNDO 对照。
- 不修改生产撤销策略、不关闭全局 UNDO、不在用户图中测试，保留全部历史失败证据。
